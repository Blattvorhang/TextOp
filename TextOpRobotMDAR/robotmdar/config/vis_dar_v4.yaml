# @package _global_
defaults:
  - vis_dar
  - _self_

data:
  goal_type: body_ext
  goal_encoding: legacy40
  goal_offset_range: null

denoiser:
  goal_encoding: ${data.goal_encoding}
  goal_dim: 21
  cond_mask_prob:
    goal:
      position: 0.1
      yaw: 0.3
      time: 0.3
      body: 0.3
