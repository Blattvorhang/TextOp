# @package _global_
defaults:
  - base
  - train/dar
  - _self_

expname: DAR-BODY-EXT-GOAL
task: train-dar

data:
  history_len: 16
  future_len: 64
  num_primitive: 4
  batch_size: 256
  goal_type: body_ext
  goal_per_primitive: true
  goal_offset: 0
  goal_offset_range: null
  goal_timestep_mode: zero
  val:
    goal_offset_range: null

denoiser:
  goal_dim: 21
  cond_goal_root_mask_prob: 0.1
  cond_goal_yaw_mask_prob: 0.3
  cond_goal_time_mask_prob: 0.0
  cond_goal_body_mask_prob: 0.3
